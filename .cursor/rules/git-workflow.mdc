---
description: Git workflow preferences - do not stage, commit, or push on behalf of the user
alwaysApply: true
---

# Git Workflow

- **Never** run `git add`, `git commit`, or `git push` unless the user explicitly asks you to.
- The user handles all git staging, committing, and pushing manually.
- When changes are complete, summarize what was changed so the user can commit with their own message.
