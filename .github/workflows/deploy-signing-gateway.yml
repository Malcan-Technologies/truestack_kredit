name: Deploy Signing Gateway

on:
  push:
    branches:
      - main
    paths:
      - "apps/signing-gateway/**"
      - "config/clients/demo-client.yaml"
      - ".github/workflows/deploy-signing-gateway.yml"
  workflow_dispatch:
    inputs:
      client_id:
        description: "Client to deploy. Add new clients to config/clients/<id>.yaml AND to this list. Push to main still auto-deploys demo-client when apps/signing-gateway/** or config/clients/demo-client.yaml changes."
        required: true
        default: "proficient-premium"
        type: choice
        options:
          - demo-client
          - proficient-premium
      platform_version:
        description: "Semantic version for the shared Pro platform (for example 1.2.3)"
        required: false
        type: string
      action:
        description: "What to do"
        required: true
        default: "full"
        type: choice
        options:
          - full
          - build-only
          - deploy-only
  # Reusable entry point so deploy-pro.yml (and any future caller) can trigger
  # a signing-gateway deploy as part of a larger Pro client release without
  # duplicating the on-prem SSH / Cloudflare / MTSA logic.
  workflow_call:
    inputs:
      client_id:
        description: "Client config id (must match config/clients/<id>.yaml)."
        required: true
        type: string
      platform_version:
        description: "Semantic version for the shared Pro platform (for example 1.2.3)."
        required: false
        type: string
      action:
        description: "full | build-only | deploy-only"
        required: false
        default: "full"
        type: string

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  load-config:
    name: Load Client Config
    runs-on: ubuntu-latest

    outputs:
      github_environment: ${{ steps.config.outputs.github_environment }}
      aws_region: ${{ steps.config.outputs.aws_region }}
      ecr_signing_gateway: ${{ steps.config.outputs.ecr_signing_gateway }}
      signing_ssh_host: ${{ steps.config.outputs.signing_ssh_host }}
      signing_mtsa_env: ${{ steps.config.outputs.signing_mtsa_env }}
      platform_release_default: ${{ steps.config.outputs.platform_release_default }}
      mtsa_build_context: ${{ steps.config.outputs.mtsa_build_context }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Determine client config path
        id: client
        run: |
          CLIENT_ID="${{ inputs.client_id || 'demo-client' }}"
          echo "client_id=$CLIENT_ID" >> "$GITHUB_OUTPUT"
          echo "config_path=config/clients/$CLIENT_ID.yaml" >> "$GITHUB_OUTPUT"

      - name: Parse client config
        id: config
        env:
          CLIENT_CONFIG_PATH: ${{ steps.client.outputs.config_path }}
        run: |
          ruby -ryaml -e '
            cfg = YAML.load_file(ENV.fetch("CLIENT_CONFIG_PATH"))
            mtsa_ctx = cfg.dig("signing", "mtsa_build_context")
            mtsa_ctx = "apps/signing-gateway/mtsa-pilot" if mtsa_ctx.nil? || mtsa_ctx.to_s.empty?

            outputs = {
              "github_environment" => cfg.dig("aws", "github_environment"),
              "aws_region" => cfg.dig("aws", "region"),
              "ecr_signing_gateway" => cfg.dig("signing", "ecr_repository"),
              "signing_ssh_host" => cfg.dig("signing", "ssh_host"),
              "signing_mtsa_env" => cfg.dig("signing", "mtsa_env"),
              "platform_release_default" => cfg.dig("deploy", "platform_release"),
              "mtsa_build_context" => mtsa_ctx,
            }

            File.open(ENV.fetch("GITHUB_OUTPUT"), "a") do |f|
              outputs.each do |key, value|
                raise "Missing config value for #{key}" if value.nil? || value.to_s.empty?
                f.puts("#{key}=#{value}")
              end
            end
          '

  resolve-release:
    name: Resolve Release
    runs-on: ubuntu-latest
    needs: load-config

    outputs:
      release_version: ${{ steps.resolve.outputs.release_version }}
      checkout_ref: ${{ steps.resolve.outputs.checkout_ref }}
      image_tag: ${{ steps.resolve.outputs.image_tag }}
      use_semver: ${{ steps.resolve.outputs.use_semver }}

    steps:
      - name: Resolve release ref
        id: resolve
        env:
          PLATFORM_INPUT: ${{ inputs.platform_version }}
          PLATFORM_DEFAULT: ${{ needs.load-config.outputs.platform_release_default }}
        run: |
          set -euo pipefail

          # Push to main = auto-deploy from current SHA (used by demo-client only).
          # workflow_dispatch and workflow_call both go through the semver path below.
          if [ "${{ github.event_name }}" = "push" ]; then
            echo "release_version=main" >> "$GITHUB_OUTPUT"
            echo "checkout_ref=${GITHUB_SHA}" >> "$GITHUB_OUTPUT"
            echo "image_tag=${GITHUB_SHA}" >> "$GITHUB_OUTPUT"
            echo "use_semver=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          SEMVER_REGEX='^[0-9]+\.[0-9]+\.[0-9]+(-[0-9A-Za-z.-]+)?$'
          RELEASE_VERSION="$PLATFORM_INPUT"
          if [ -z "$RELEASE_VERSION" ]; then
            RELEASE_VERSION="$PLATFORM_DEFAULT"
          fi

          if [ -z "$RELEASE_VERSION" ]; then
            echo "platform_version is required either as workflow input or config/clients/<client>.yaml deploy.platform_release"
            exit 1
          fi

          if [ "$RELEASE_VERSION" = "latest" ]; then
            echo "release_version=latest" >> "$GITHUB_OUTPUT"
            echo "checkout_ref=${GITHUB_SHA}" >> "$GITHUB_OUTPUT"
            echo "image_tag=latest" >> "$GITHUB_OUTPUT"
            echo "use_semver=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          if ! [[ "$RELEASE_VERSION" =~ $SEMVER_REGEX ]]; then
            echo "Invalid platform_version: $RELEASE_VERSION"
            echo "Expected semantic version like 1.2.3 or 1.2.3-rc.1"
            exit 1
          fi

          echo "release_version=$RELEASE_VERSION" >> "$GITHUB_OUTPUT"
          echo "checkout_ref=refs/tags/pro-platform-v$RELEASE_VERSION" >> "$GITHUB_OUTPUT"
          echo "image_tag=$RELEASE_VERSION" >> "$GITHUB_OUTPUT"
          echo "use_semver=true" >> "$GITHUB_OUTPUT"

  build-and-push:
    name: Build and Push Signing Gateway
    runs-on: ubuntu-latest
    needs: [load-config, resolve-release]
    environment: ${{ needs.load-config.outputs.github_environment }}
    # Skip the docker build for the deploy-only path (re-roll the existing image).
    # On push events `inputs.action` is empty -> evaluates true -> build runs.
    if: inputs.action != 'deploy-only'

    env:
      AWS_REGION: ${{ needs.load-config.outputs.aws_region }}
      ECR_REPOSITORY: ${{ needs.load-config.outputs.ecr_signing_gateway }}

    permissions:
      id-token: write
      contents: read

    outputs:
      image_tag: ${{ steps.build.outputs.image_tag }}
      ecr_registry: ${{ steps.login-ecr.outputs.registry }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v5
        with:
          ref: ${{ needs.resolve-release.outputs.checkout_ref }}
          fetch-depth: 0

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_ROLE_ARN }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2

      - name: Build and push signing gateway image
        id: build
        env:
          ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
          IMAGE_TAG: ${{ needs.resolve-release.outputs.image_tag }}
          USE_SEMVER: ${{ needs.resolve-release.outputs.use_semver }}
        run: |
          SHORT_SHA=$(git rev-parse --short HEAD)
          docker build -f apps/signing-gateway/Dockerfile \
            -t "$ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG" \
            .
          docker push "$ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG"

          if [ "$USE_SEMVER" = "true" ]; then
            docker tag "$ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG" \
              "$ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG-$SHORT_SHA"
            docker push "$ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG-$SHORT_SHA"
          else
            docker tag "$ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG" \
              "$ECR_REGISTRY/$ECR_REPOSITORY:latest"
            docker push "$ECR_REGISTRY/$ECR_REPOSITORY:latest"
          fi

          echo "image_tag=$IMAGE_TAG" >> "$GITHUB_OUTPUT"

      - name: Build and push MTSA pilot image
        env:
          ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
          IMAGE_TAG: ${{ needs.resolve-release.outputs.image_tag }}
          USE_SEMVER: ${{ needs.resolve-release.outputs.use_semver }}
          MTSA_CTX: ${{ needs.load-config.outputs.mtsa_build_context }}
        run: |
          set -euo pipefail
          WAR_PATH="${MTSA_CTX}/webapps/MTSAPilot.war"
          if [ ! -f "$WAR_PATH" ]; then
            echo "::error::Missing $WAR_PATH — pilot MTSA image cannot build. Add Trustgate MTSAPilot.war under that directory (see signing.mtsa_build_context in config/clients/<client>.yaml)."
            exit 1
          fi
          SHORT_SHA=$(git rev-parse --short HEAD)
          MTSA_TAG="mtsa-${IMAGE_TAG}"
          docker build -f "${MTSA_CTX}/Dockerfile" \
            -t "$ECR_REGISTRY/$ECR_REPOSITORY:$MTSA_TAG" \
            "$MTSA_CTX"
          docker push "$ECR_REGISTRY/$ECR_REPOSITORY:$MTSA_TAG"

          if [ "$USE_SEMVER" = "true" ]; then
            docker tag "$ECR_REGISTRY/$ECR_REPOSITORY:$MTSA_TAG" \
              "$ECR_REGISTRY/$ECR_REPOSITORY:mtsa-${IMAGE_TAG}-${SHORT_SHA}"
            docker push "$ECR_REGISTRY/$ECR_REPOSITORY:mtsa-${IMAGE_TAG}-${SHORT_SHA}"
          else
            docker tag "$ECR_REGISTRY/$ECR_REPOSITORY:$MTSA_TAG" \
              "$ECR_REGISTRY/$ECR_REPOSITORY:mtsa-latest"
            docker push "$ECR_REGISTRY/$ECR_REPOSITORY:mtsa-latest"
          fi

  deploy-to-onprem:
    name: Deploy to On-Prem Server
    runs-on: ubuntu-latest
    needs: [load-config, resolve-release, build-and-push]
    environment: ${{ needs.load-config.outputs.github_environment }}
    if: |
      always() &&
      needs.load-config.result == 'success' &&
      needs.resolve-release.result == 'success' &&
      (needs.build-and-push.result == 'success' || inputs.action == 'deploy-only') &&
      inputs.action != 'build-only'

    env:
      AWS_REGION: ${{ needs.load-config.outputs.aws_region }}
      ECR_REPOSITORY: ${{ needs.load-config.outputs.ecr_signing_gateway }}
      SSH_HOST: ${{ needs.load-config.outputs.signing_ssh_host }}
      SIGNING_MTSA_ENV: ${{ needs.load-config.outputs.signing_mtsa_env }}

    permissions:
      id-token: write
      contents: read

    steps:
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_ROLE_ARN }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2

      - name: Get ECR login password
        id: ecr-password
        run: |
          ECR_PASSWORD=$(aws ecr get-login-password --region "$AWS_REGION")
          echo "::add-mask::$ECR_PASSWORD"
          echo "password=$ECR_PASSWORD" >> "$GITHUB_OUTPUT"

      - name: Determine image tag
        id: tag
        run: |
          if [ "${{ inputs.action }}" = "deploy-only" ]; then
            echo "tag=${{ needs.resolve-release.outputs.image_tag }}" >> "$GITHUB_OUTPUT"
          else
            echo "tag=${{ needs.build-and-push.outputs.image_tag }}" >> "$GITHUB_OUTPUT"
          fi

      - name: Derive MTSA WSDL path
        id: mtsa
        run: |
          if [ "$SIGNING_MTSA_ENV" = "prod" ]; then
            echo "wsdl_path=/MTSA/MyTrustSignerAgentWSAPv2?wsdl" >> "$GITHUB_OUTPUT"
          else
            echo "wsdl_path=/MTSAPilot/MyTrustSignerAgentWSAPv2?wsdl" >> "$GITHUB_OUTPUT"
          fi

      - name: Resolve MTSA Docker image for on-prem
        id: mtsa_image
        env:
          ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
          ECR_REPOSITORY: ${{ env.ECR_REPOSITORY }}
          IMAGE_TAG: ${{ steps.tag.outputs.tag }}
          ONPREM_MTSA_IMAGE: ${{ secrets.ONPREM_MTSA_IMAGE }}
        run: |
          if [ -n "${ONPREM_MTSA_IMAGE}" ]; then
            echo "ref=${ONPREM_MTSA_IMAGE}" >> "$GITHUB_OUTPUT"
          else
            echo "ref=${ECR_REGISTRY}/${ECR_REPOSITORY}:mtsa-${IMAGE_TAG}" >> "$GITHUB_OUTPUT"
          fi

      - name: Build on-prem .env file
        id: envfile
        env:
          ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
          IMAGE_TAG: ${{ steps.tag.outputs.tag }}
          MTSA_WSDL_PATH: ${{ steps.mtsa.outputs.wsdl_path }}
        run: |
          ENV_CONTENT=$(cat <<'HEREDOC_END'
          ECR_REGISTRY=${{ steps.login-ecr.outputs.registry }}
          GATEWAY_TAG=${{ steps.tag.outputs.tag }}
          MTSA_SOAP_USERNAME=${{ secrets.ONPREM_MTSA_USERNAME }}
          MTSA_SOAP_PASSWORD=${{ secrets.ONPREM_MTSA_PASSWORD }}
          MTSA_WSDL_PATH=${{ steps.mtsa.outputs.wsdl_path }}
          MTSA_IMAGE=${{ steps.mtsa_image.outputs.ref }}
          SIGNING_API_KEY=${{ secrets.ONPREM_SIGNING_API_KEY }}
          SIGNING_GATEWAY_PUBLIC_IP=${{ secrets.ONPREM_SIGNING_GATEWAY_PUBLIC_IP }}
          GATEWAY_PORT=3100
          BACKUP_ENABLED=true
          CF_TUNNEL_TOKEN=${{ secrets.ONPREM_CF_TUNNEL_TOKEN }}
          HEREDOC_END
          )

          echo "::add-mask::${{ secrets.ONPREM_MTSA_USERNAME }}"
          echo "::add-mask::${{ secrets.ONPREM_MTSA_PASSWORD }}"
          echo "::add-mask::${{ secrets.ONPREM_SIGNING_API_KEY }}"
          echo "::add-mask::${{ secrets.ONPREM_CF_TUNNEL_TOKEN }}"

          ENV_B64=$(echo "$ENV_CONTENT" | base64 -w 0)
          echo "b64=$ENV_B64" >> "$GITHUB_OUTPUT"

      - name: Install cloudflared
        run: |
          curl -fsSL https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-linux-amd64 \
            -o /usr/local/bin/cloudflared
          chmod +x /usr/local/bin/cloudflared

      - name: Checkout code (sync deploy script to on-prem)
        uses: actions/checkout@v5

      - name: Deploy via SSH through Cloudflare Tunnel
        env:
          CF_ACCESS_CLIENT_ID: ${{ secrets.CF_ACCESS_CLIENT_ID }}
          CF_ACCESS_CLIENT_SECRET: ${{ secrets.CF_ACCESS_CLIENT_SECRET }}
          ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
          ECR_PASSWORD: ${{ steps.ecr-password.outputs.password }}
          IMAGE_TAG: ${{ steps.tag.outputs.tag }}
          ENV_B64: ${{ steps.envfile.outputs.b64 }}
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.ONPREM_SSH_KEY }}" > ~/.ssh/deploy_key
          chmod 600 ~/.ssh/deploy_key

          cat >> ~/.ssh/config <<EOF
          Host onprem-signing
            HostName $SSH_HOST
            User deploy
            IdentityFile ~/.ssh/deploy_key
            ProxyCommand cloudflared access ssh --hostname %h --id $CF_ACCESS_CLIENT_ID --secret $CF_ACCESS_CLIENT_SECRET
            StrictHostKeyChecking no
          EOF

          scp scripts/signing-gateway/deploy.sh onprem-signing:/opt/signing-stack/deploy.sh
          ssh onprem-signing "chmod +x /opt/signing-stack/deploy.sh"
          ssh onprem-signing "echo '$ENV_B64' | base64 -d > /opt/signing-stack/.env && ECR_REGISTRY='$ECR_REGISTRY' ECR_REPOSITORY='$ECR_REPOSITORY' ECR_PASSWORD='$ECR_PASSWORD' /opt/signing-stack/deploy.sh '$IMAGE_TAG'"

  summary:
    name: Deployment Summary
    runs-on: ubuntu-latest
    needs: [load-config, resolve-release, build-and-push, deploy-to-onprem]
    if: always()

    steps:
      - name: Summary
        run: |
          echo "Trigger: ${{ github.event_name }}"
          echo "Client: ${{ inputs.client_id || 'demo-client' }}"
          echo "Release: ${{ needs.resolve-release.outputs.release_version }}"
          echo "Action: ${{ inputs.action || 'auto (full)' }}"
          echo "Build: ${{ needs.build-and-push.result }}"
          echo "Deploy: ${{ needs.deploy-to-onprem.result }}"
