/**
 * e-KYC identity-locked callout (profile view, apply flow, profile edit, mobile).
 * Keep in sync with `apps/borrower_pro_mobile/.../lib/identity-ekyc-copy.ts`.
 */
export const IDENTITY_EKYC_LOCKED_BANNER_TEXT =
  "Your identity has been verified by e-KYC. Your name, IC, date of birth and gender are locked. Redo e-KYC from your Profile if any of these need updating.";
