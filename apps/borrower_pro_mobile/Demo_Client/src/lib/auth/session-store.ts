/**
 * Session token constants.
 *
 * The Better Auth Expo client (expoClient) handles all SecureStore read/write
 * automatically. This file is kept for the cookie name constant used in debugging.
 */

/** The cookie name Better Auth uses for borrower sessions (matches server-side cookiePrefix). */
export const SESSION_COOKIE_NAME = 'truestack-borrower.session_token';
